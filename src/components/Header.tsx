import React from "react";

type Props = {};

export const Header = (props: Props) => {
  return (
    <header className="w-full pt-2 pb-6">
      <h1 className="uppercase text-2xl font-semibold">super film</h1>
    </header>
  );
};
